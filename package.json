{
  "name": "cat-legion",
  "version": "1.0.0",
  "description": "Idle RPG de gatos - HTML5 Canvas + vanillaJS",
  "main": "index.html",
  "scripts": {
    "start": "npx serve .",
    "cap:add": "npx cap add android",
    "cap:sync": "npx cap sync",
    "cap:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/android": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}